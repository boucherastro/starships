__version__ = '1.0.0'

# stellar_fit is imported lazily to avoid mandatory dependencies
# (phoenix_models, PyAstronomy) that are only available on the cluster.
# Use:  import starships.stellar_fit as sf
