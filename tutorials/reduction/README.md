# Reduction tutorials

- `Reduction_parameters_explained.ipynb`: Explains every data reduction parameter
  (`pipeline/reduction.py`, `starships.transpec.ReductionParams`) and shows their effect on a
  real dataset. No petitRADTRANS needed, runs anywhere raw FITS files are available.
